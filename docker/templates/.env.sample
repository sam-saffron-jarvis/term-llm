# term-llm agent: {{AGENT_NAME}}

# Path to the term-llm git repo (used as Docker build context)
TERM_LLM_REPO=

# API keys — at least one provider is required
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# VENICE_API_KEY=

# Web UI settings
WEB_PORT=8081
WEB_TOKEN={{WEB_TOKEN}}
WEB_BASE_PATH=/chat
