name: jarvis
description: "Your personal AI assistant"

tools:
  enabled: [read_file, write_file, edit_file, glob, grep, shell, ask_user, spawn_agent, view_image, run_agent_script]

shell:
  auto_run: true
  allow: ["*"]

max_turns: 200
search: true

agents_md: "false"
