# term-llm contain workspace: {{name}}

This workspace is managed by Docker Compose. Edit `compose.yaml` directly.

## Next steps

Start with:

```sh
term-llm contain start {{name}}
```

Open a shell with:

```sh
term-llm contain shell {{name}}
```

Stop with:

```sh
term-llm contain stop {{name}}
```
